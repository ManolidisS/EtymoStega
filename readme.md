Source code for EtymoStega.
Will be updated soon...

# Basic Setup
```bash
git clone https://github.com/ManolidisS/EtymoStega [FOLDER]
cd [FOLDER]
cp config.example.yaml config.yaml
nano config.yaml
pip install -r requirements.txt
python -m setup
```